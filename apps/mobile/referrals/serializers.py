# Unused — ReferralDataView (views.py) builds its response dict directly
# since it matches one specific nested shape (mockApi.getReferralData())
# rather than a plain model serialization. Kept as an empty module only
# because this file couldn't be deleted in the authoring environment.
